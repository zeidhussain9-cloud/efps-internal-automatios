import {createServer} from "node:http";import {readFile,stat} from "node:fs/promises";import {join,extname,resolve} from "node:path";const root=resolve("dist");const mime={".html":"text/html; charset=utf-8",".js":"text/javascript; charset=utf-8",".css":"text/css; charset=utf-8",".svg":"image/svg+xml",".png":"image/png",".ico":"image/x-icon"};createServer(async(req,res)=>{try{let p=decodeURIComponent(new URL(req.url,"http://localhost").pathname);if(p==="/health"){res.writeHead(200,{"Content-Type":"application/json"});return res.end(JSON.stringify({ok:true,mode:"synthetic"}));}let f=resolve(join(root,p));if(!f.startsWith(root+"/")&&f!==root)throw Error("bad path");let s=await stat(f).catch(()=>null);if(!s||!s.isFile())f=join(root,"index.html");const data=await readFile(f);res.writeHead(200,{"Content-Type":mime[extname(f)]||"application/octet-stream","X-Content-Type-Options":"nosniff"});res.end(data);}catch(e){res.writeHead(500);res.end("CRM unavailable");}}).listen(Number(process.env.PORT||10000),"0.0.0.0");